import { Box } from "@mui/system";

const KeyFigureBar = () => {
  return (
    <Box
      style={{
        width: "100%",
        height: "0.6rem",
        backgroundColor: "#C4C4C4",
      }}
    ></Box>
  );
};

export default KeyFigureBar;
